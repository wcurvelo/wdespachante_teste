<?xml version="1.0" encoding="UTF-8"?>
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">
    <url>
        <loc>{{ url_for('index', _external=True) }}</loc>
        <lastmod>{{ current_date.strftime('%Y-%m-%d') }}</lastmod>
        <changefreq>hourly</changefreq>
        <priority>1.0</priority>
    </url>
    <url>
        <loc>{{ url_for('blog', _external=True) }}</loc>
        <lastmod>{{ current_date.strftime('%Y-%m-%d') }}</lastmod>
        <changefreq>daily</changefreq>
        <priority>0.8</priority>
    </url>
    {% for article in articles %}
    <url>
        <loc>{{ url_for('news_detail', slug=article.slug, _external=True) }}</loc>
        <lastmod>{{ article.publication_date.strftime('%Y-%m-%d') }}</lastmod>
        <changefreq>weekly</changefreq>
        <priority>0.7</priority>
    </url>
    {% endfor %}
</urlset> 